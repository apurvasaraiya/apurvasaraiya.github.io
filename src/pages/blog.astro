---
import Base from '../layouts/Base.astro';
import Card from '../components/Card.astro';

const posts = [
  {
    title: 'Building Accessible Web Applications',
    excerpt: 'Accessibility isn\'t just a nice-to-have—it\'s essential. Learn the key principles and techniques for creating inclusive web experiences that work for everyone.',
    date: 'Dec 5, 2024',
    href: '#',
    tags: ['Accessibility', 'Web Dev'],
  },
  {
    title: 'The Art of Clean Code',
    excerpt: 'Writing code that works is just the beginning. Discover tips and best practices for writing maintainable, readable, and elegant code that your future self will thank you for.',
    date: 'Nov 28, 2024',
    href: '#',
    tags: ['Best Practices', 'Code Quality'],
  },
  {
    title: 'Modern CSS Techniques You Should Know',
    excerpt: 'CSS has evolved dramatically. From container queries to cascade layers, explore the modern features that are changing how we style the web.',
    date: 'Nov 15, 2024',
    href: '#',
    tags: ['CSS', 'Frontend'],
  },
  {
    title: 'Getting Started with Astro',
    excerpt: 'Astro is changing how we think about building websites. Here\'s everything you need to know to get started with this exciting framework.',
    date: 'Nov 1, 2024',
    href: '#',
    tags: ['Astro', 'Tutorial'],
  },
  {
    title: 'Performance Optimization Deep Dive',
    excerpt: 'Speed matters. Learn advanced techniques for measuring, analyzing, and improving the performance of your web applications.',
    date: 'Oct 20, 2024',
    href: '#',
    tags: ['Performance', 'Web Dev'],
  },
  {
    title: 'My Developer Setup 2024',
    excerpt: 'A look at the tools, extensions, and workflows I use daily to stay productive. Plus some tips for setting up your own optimal dev environment.',
    date: 'Oct 10, 2024',
    href: '#',
    tags: ['Productivity', 'Tools'],
  },
];
---

<Base title="Blog - YourName" description="Thoughts, tutorials, and insights about web development, design, and technology.">
  <!-- Page Header -->
  <section class="page-header">
    <div class="page-header-bg">
      <div class="blob blob-2 header-blob-1"></div>
      <div class="blob blob-1 header-blob-2"></div>
    </div>
    <div class="container">
      <h1 class="animate-fade-in">Blog</h1>
      <p class="page-subtitle animate-fade-in delay-1">
        Thoughts, tutorials, and things I've learned along the way. 
        Writing helps me understand things better! ✍️
      </p>
    </div>
  </section>

  <!-- Posts List -->
  <section class="section posts-section">
    <div class="container">
      <div class="posts-grid">
        {posts.map((post, i) => (
          <article class={`post-card animate-fade-in delay-${(i % 3) + 1}`}>
            <div class="post-accent"></div>
            <div class="post-content">
              <div class="post-meta">
                <time class="post-date">{post.date}</time>
                <div class="post-tags">
                  {post.tags.map(tag => (
                    <span class="tag">{tag}</span>
                  ))}
                </div>
              </div>
              
              <h2 class="post-title">
                <a href={post.href}>{post.title}</a>
              </h2>
              
              <p class="post-excerpt">{post.excerpt}</p>
              
              <a href={post.href} class="read-more">
                Read More
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                  <line x1="5" y1="12" x2="19" y2="12"></line>
                  <polyline points="12 5 19 12 12 19"></polyline>
                </svg>
              </a>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- Newsletter Section -->
  <section class="section newsletter">
    <div class="container">
      <div class="newsletter-card">
        <div class="newsletter-blob blob blob-2"></div>
        <div class="newsletter-content">
          <span class="newsletter-icon">📬</span>
          <h2>Stay Updated</h2>
          <p>Get notified when I publish new posts. No spam, just good content!</p>
          <form class="newsletter-form" onsubmit="event.preventDefault(); alert('Thanks for subscribing!');">
            <input 
              type="email" 
              placeholder="your@email.com" 
              required 
              class="newsletter-input"
              aria-label="Email address"
            />
            <button type="submit" class="btn btn-primary">Subscribe</button>
          </form>
        </div>
      </div>
    </div>
  </section>
</Base>

<style>
  /* Page Header */
  .page-header {
    position: relative;
    padding: var(--space-3xl) 0 var(--space-2xl);
    text-align: center;
    overflow: hidden;
  }
  
  .page-header-bg {
    position: absolute;
    inset: 0;
    z-index: -1;
  }
  
  .header-blob-1 {
    width: 250px;
    height: 250px;
    top: -80px;
    left: 10%;
  }
  
  .header-blob-2 {
    width: 180px;
    height: 180px;
    bottom: -40px;
    right: 15%;
  }
  
  .page-subtitle {
    font-size: 1.25rem;
    color: var(--color-text-muted);
    max-width: 600px;
    margin: var(--space-md) auto 0;
  }
  
  /* Posts Grid */
  .posts-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
    max-width: 800px;
    margin: 0 auto;
  }
  
  .post-card {
    background: var(--color-bg);
    border: 3px solid var(--color-text);
    border-radius: var(--radius-md);
    overflow: hidden;
    transition: all var(--transition-bounce);
    box-shadow: var(--shadow-cartoon);
  }
  
  .post-card:hover {
    transform: translate(-4px, -4px);
    box-shadow: var(--shadow-cartoon-lg);
  }
  
  .post-accent {
    height: 6px;
    background: linear-gradient(90deg, var(--color-tertiary), var(--color-secondary));
  }
  
  .post-content {
    padding: var(--space-xl);
  }
  
  .post-meta {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-md);
    flex-wrap: wrap;
  }
  
  .post-date {
    font-size: 0.875rem;
    color: var(--color-text-muted);
  }
  
  .post-tags {
    display: flex;
    gap: var(--space-xs);
  }
  
  .post-tags .tag {
    font-size: 0.7rem;
    padding: 0.2rem 0.5rem;
  }
  
  .post-title {
    font-size: 1.5rem;
    margin-bottom: var(--space-sm);
  }
  
  .post-title a {
    color: inherit;
    text-decoration: none;
    transition: color var(--transition-fast);
  }
  
  .post-title a:hover {
    color: var(--color-accent);
  }
  
  .post-excerpt {
    color: var(--color-text-muted);
    line-height: 1.7;
    margin-bottom: var(--space-md);
  }
  
  .read-more {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    font-family: var(--font-display);
    font-weight: 600;
    color: var(--color-accent);
    transition: gap var(--transition-fast);
  }
  
  .read-more:hover {
    gap: var(--space-sm);
  }
  
  /* Newsletter */
  .newsletter {
    padding-bottom: var(--space-3xl);
  }
  
  .newsletter-card {
    position: relative;
    max-width: 600px;
    margin: 0 auto;
    background: var(--color-bg-alt);
    border: 3px solid var(--color-text);
    border-radius: var(--radius-lg);
    padding: var(--space-2xl);
    text-align: center;
    overflow: hidden;
    box-shadow: var(--shadow-cartoon);
  }
  
  .newsletter-blob {
    width: 150px;
    height: 150px;
    top: -50px;
    right: -40px;
    opacity: 0.4;
  }
  
  .newsletter-content {
    position: relative;
  }
  
  .newsletter-icon {
    font-size: 3rem;
    display: block;
    margin-bottom: var(--space-md);
  }
  
  .newsletter-content h2 {
    margin-bottom: var(--space-sm);
  }
  
  .newsletter-content p {
    color: var(--color-text-muted);
    margin-bottom: var(--space-lg);
  }
  
  .newsletter-form {
    display: flex;
    gap: var(--space-sm);
    max-width: 400px;
    margin: 0 auto;
  }
  
  .newsletter-input {
    flex: 1;
    padding: var(--space-sm) var(--space-md);
    font-family: var(--font-body);
    font-size: 1rem;
    border: 3px solid var(--color-text);
    border-radius: var(--radius-md);
    background: var(--color-bg);
    transition: box-shadow var(--transition-fast);
  }
  
  .newsletter-input:focus {
    outline: none;
    box-shadow: var(--shadow-cartoon);
  }
  
  @media (max-width: 480px) {
    .newsletter-form {
      flex-direction: column;
    }
  }
</style>

