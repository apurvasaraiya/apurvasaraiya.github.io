---
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer-blob blob blob-2"></div>
  <div class="container footer-container">
    <div class="footer-content">
      <div class="footer-brand">
        <span class="footer-logo"><span class="footer-logo-icon">&lt;/&gt;</span> Apurva Saraiya</span>
        <p class="footer-tagline">Building robust systems, one commit at a time.</p>
      </div>
      
      <div class="footer-bottom">
        <p>
          Made with 
          <span class="heart" aria-label="love">💖</span> 
          and lots of 
          <span aria-label="coffee">☕</span>
        </p>
        <p class="copyright">© {currentYear} Apurva Saraiya. All rights reserved.</p>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    position: relative;
    margin-top: auto;
    padding: var(--space-3xl) 0 var(--space-xl);
    background: var(--color-bg-alt);
    border-top: 3px solid var(--color-text);
    overflow: hidden;
  }
  
  .footer-blob {
    width: 300px;
    height: 300px;
    top: -100px;
    right: -50px;
    opacity: 0.3;
  }
  
  .footer-container {
    position: relative;
  }
  
  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: var(--space-2xl);
    margin-bottom: 0;
  }
  
  .footer-brand {
    max-width: 300px;
  }
  
  .footer-logo {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: var(--space-sm);
  }
  
  .footer-logo-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--color-accent), #ff8e8e);
    color: white;
    font-family: 'Courier New', monospace;
    font-size: 0.85rem;
    font-weight: 700;
    padding: 0.2rem 0.4rem;
    border-radius: 6px;
    letter-spacing: -1px;
  }
  
  .footer-tagline {
    color: var(--color-text-muted);
    margin-bottom: 0;
  }
  
  .footer-links {
    display: flex;
    gap: var(--space-3xl);
  }
  
  .footer-col h4 {
    font-family: var(--font-display);
    font-size: 1rem;
    margin-bottom: var(--space-md);
    color: var(--color-text);
  }
  
  .footer-col ul {
    list-style: none;
  }
  
  .footer-col li {
    margin-bottom: var(--space-xs);
  }
  
  .footer-connect {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }
  
  .footer-social-link {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    color: var(--color-text-muted);
    transition: all var(--transition-bounce);
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-md);
    border: 2px solid transparent;
  }
  
  .footer-social-link:hover {
    color: var(--color-accent);
    background: var(--color-bg);
    border-color: var(--color-text);
    transform: translateX(4px);
  }
  
  .footer-social-link svg {
    flex-shrink: 0;
    width: 20px;
    height: 20px;
  }
  
  .footer-social-link span {
    font-family: var(--font-display);
    font-weight: 600;
  }
  
  .footer-col a {
    color: var(--color-text-muted);
    transition: color var(--transition-fast);
  }
  
  .footer-col a:hover {
    color: var(--color-accent);
  }
  
  .footer-bottom {
    text-align: right;
    padding-top: 0;
    border-top: none;
    margin-top: 0;
  }
  
  .footer-bottom p {
    margin-bottom: var(--space-xs);
    color: var(--color-text-muted);
  }
  
  .heart {
    display: inline-block;
    animation: pulse 1s ease-in-out infinite;
  }
  
  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.2); }
  }
  
  .copyright {
    font-size: 0.875rem;
  }
  
  @media (max-width: 768px) {
    .footer-content {
      flex-direction: column;
      text-align: center;
      align-items: center;
    }
    
    .footer-brand {
      max-width: none;
    }
    
    .footer-bottom {
      text-align: center;
    }
    
    .footer-links {
      justify-content: center;
    }
  }
</style>
